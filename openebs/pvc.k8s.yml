apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openebs-mayastor-pvc
spec:
  storageClassName: openebs-triple-replica
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
